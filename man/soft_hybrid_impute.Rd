% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Soft_Hybrid_Imputation.R
\name{soft_hybrid_impute}
\alias{soft_hybrid_impute}
\title{Soft Hybrid Imputation for Proteomics Missing Values}
\usage{
soft_hybrid_impute(
  df_rf,
  df_minProb,
  df_raw,
  r0 = NULL,
  x0 = NULL,
  a = 10,
  b = 5,
  lambda = 0.5,
  visualize = TRUE,
  group_name = NULL,
  save_dir = "./SoftHybrid_DIA"
)
}
\arguments{
\item{df_rf}{A matrix/data.frame of RF-imputed values (rows = features, cols = samples).}

\item{df_minProb}{A matrix/data.frame of MinProb-imputed values (same dim/rownames as \code{df_rf}).}

\item{df_raw}{The raw matrix/data.frame with NAs (used to compute missing rate & mean intensity).}

\item{r0}{Optional numeric, elbow x (missing-rate) override. If NULL, auto-estimated.}

\item{x0}{Optional numeric, elbow y (mean-intensity) override. If NULL, auto-estimated.}

\item{a}{Steepness for missing-rate sigmoid (default 10).}

\item{b}{Steepness for intensity sigmoid (default 5).}

\item{lambda}{Trade-off between missing-rate and intensity (0–1, default 0.5).}

\item{visualize}{Logical. If TRUE, saves a TIFF + Rdata of the weight plot.}

\item{group_name}{Optional character used in plot titles/filenames.}

\item{save_dir}{Directory to save plots (default "./SoftHybrid_DIA").}
}
\value{
A matrix with soft-hybrid imputed values (same dim as inputs).
}
\description{
Blends Random Forest (RF) and MinProb imputations with a soft weight driven by
missing rate (MNAR proxy) and mean intensity (abundance). The missing-rate elbow
is estimated by LOESS + farthest-point (Kneedle-like) unless 75\\% of proteins
have zero missing (fallback to 1/n rule).
}
\examples{
\dontrun{
imp <- soft_hybrid_impute(df_rf, df_minProb, df_raw, visualize = FALSE)
}
}
